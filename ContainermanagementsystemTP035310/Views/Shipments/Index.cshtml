@model IEnumerable<ContainermanagementsystemTP034963.Models.Shipment>

@{
    ViewBag.Title = "Shipments";
}

<h2>Shipments</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cargoid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.departuretime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.arrivaltime)
        </th>
        <th>
            source
        </th>
        <th>
            destination
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cargoid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.departuretime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.arrivaltime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Warehouse.location)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Warehouse1.location)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
